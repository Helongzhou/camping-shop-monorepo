---
description: 编写或修改 NestJS 后端服务时自动激活
globs: apps/api/**/*.ts
alwaysApply: false
---
# NestJS 开发规范（2026 生产级标准）

## 1. 核心分层原则
- **Controller**: 只负责接收请求、校验 Body（通过 `ValidationPipe`）、调用 Service，并返回统一响应格式。不写任何业务逻辑。
- **Service**: 纯粹的业务逻辑与事务隔离层。
- **Module**: 每个独立业务必须模块化，导出需要共享的 Service。

## 2. 错误捕获与防御性编程
- 全局必须配置 `HttpExceptionFilter`。
- 业务错误一律抛出 Nest 标准异常（如 `throw new BadRequestException('订单已存在')`），禁止自行 `return { code: 500 }`。
- 涉及金钱计算、库存扣减等操作，必须在 Service 层注入 Prisma/Drizzle 事务处理。

## 3. 示例：标准控制器与异常格式
```typescript
@Controller('orders')
export class OrdersController {
  constructor(private readonly ordersService: OrdersService) {}

  @Post()
  @UsePipes(new ValidationPipe({ whitelist: true }))
  async createOrder(@Body() createOrderDto: CreateOrderDto): Promise<StandardResponse<Order>> {
    const order = await this.ordersService.create(createOrderDto);
    return { success: true, data: order };
  }
}