# Frontend Guidelines — Not Applicable

> `@myshop/db` is a database package with no UI.

---

## Overview

No frontend code exists under `packages/db/`. Use `spec/web/frontend/` for UI conventions.

This directory is Trellis scaffolding only — skip for `@myshop/db` tasks.
